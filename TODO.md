1. At the end verify, that all solutions are valid etc.
2. Try to find better way of convering hashes to indexes in custom-map to limit collisions (example at the end of Scenario 2 in tests)
3. Fix differences between TS checks in VSC and in Jest in custom-map (reason for all those type assertions)
4. Create directory-specific file names instead of index.ts everywhere.
5. Create github actions and include jest tests in them
6. Research: Tail Recursion
7. Idea: Revolut algorithm for sending back money
8. Idea: Some linked lists exercises, like reverting etc.
